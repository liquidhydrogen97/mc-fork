---
contract_id: "D1.2"
mode: development
status: draft
assigned_to: "{agent_id}"
created: "2026-05-03T00:00:00Z"
updated: "2026-05-03T00:00:00Z"
multiplicity: 1
branch_id: main
autonomous: false
---

# Contract: D1.2 -- z_clamped_gradient density function

## Objective

Implement and register a custom density function type `mc_fork:z_clamped_gradient` in the NeoForge mod, mirroring vanilla `minecraft:y_clamped_gradient` but reading the Z world coordinate instead of Y. Update the datapack file `datapack/data/mc_fork/worldgen/density_function/latitude_bias.json` to use this new type with the piecewise constants from `worldgen-plan.md` Phase 1. After this contract is accepted, the latitude climate gradient activates: temperature actually varies with z position, biome distribution shifts as the player travels north/south.

## Mode

development

Development contracts produce code artifacts: implementations, integrations, tools, libraries, and infrastructure. TDD is mandatory. Tests are written before implementation. Security review is triggered when the Security Scope section lists any sensitive surfaces.

## Specification

**Dependency:** D1.1 (NeoForge mod skeleton) MUST be accepted before this contract can be assigned. The teammate working D1.2 reads D1.1's mod build into their working tree.

**Output files:**

- `mod/src/main/java/com/liquidhydrogen/mcfork/worldgen/density/ZClampedGradient.java` — the custom density function record/class
- `mod/src/main/java/com/liquidhydrogen/mcfork/worldgen/density/ModDensityFunctions.java` — registration class with `DeferredRegister.create(BuiltInRegistries.DENSITY_FUNCTION_TYPE, "mc_fork")` pattern
- `mod/src/test/java/com/liquidhydrogen/mcfork/worldgen/density/ZClampedGradientTest.java` — JUnit 5 unit tests
- Modification to `mod/src/main/java/com/liquidhydrogen/mcfork/McForkMod.java` to invoke the registration in mod constructor
- Update to `datapack/data/mc_fork/worldgen/density_function/latitude_bias.json` — replace the constant placeholder with a `mc_fork:z_clamped_gradient` reference

**Behavior of `ZClampedGradient`:**

The function takes four parameters: `from_z` (double), `to_z` (double), `from_value` (double), `to_value` (double). For a given world position with z coordinate `z_pos`:

```
if z_pos <= from_z: return from_value
if z_pos >= to_z:   return to_value
otherwise:          return from_value + (z_pos - from_z) * (to_value - from_value) / (to_z - from_z)
```

This must work for both positive and negative `from_z`, `to_z`, and z_pos. The function is symmetric around z=0 only if the operator's piecewise spec is built from two compositions (one for positive z, one for negative z, combined via `minecraft:add` and `minecraft:abs` or equivalent).

**Implementation pattern:** Mirror NeoForge's `YClampedGradient` source (in the official Mojang mappings: `net.minecraft.world.level.levelgen.DensityFunctions$YClampedGradient`). Use a record with a static `Codec` field. Implement `DensityFunction.SimpleFunction` or the appropriate sub-interface for stateless density functions. Register the type via `DeferredRegister<MapCodec<? extends DensityFunction>>` against `BuiltInRegistries.DENSITY_FUNCTION_TYPE`.

**Codec / JSON shape:** Match vanilla `y_clamped_gradient` conventions:

```json
{
  "type": "mc_fork:z_clamped_gradient",
  "from_z": -300000,
  "to_z": 300000,
  "from_value": 1.0,
  "to_value": -1.0
}
```

(Z=-300000 returns `from_value=1.0`, z=+300000 returns `to_value=-1.0`, intermediate z linearly interpolated, beyond clamps.)

**Datapack update — `latitude_bias.json`:** Replace current constant placeholder with a piecewise composition that implements the spec from `worldgen-plan.md` Phase 1 latitude bias formula. The simplest correct form:

```json
{
  "type": "minecraft:add",
  "argument1": {
    "type": "mc_fork:z_clamped_gradient",
    "from_z": 0,
    "to_z": 300000,
    "from_value": 0.0,
    "to_value": -0.7
  },
  "argument2": {
    "type": "mc_fork:z_clamped_gradient",
    "from_z": 0,
    "to_z": -300000,
    "from_value": 0.0,
    "to_value": -0.7
  }
}
```

This produces 0 bias at z=0, -0.7 bias at |z|=300k, monotonic between. The hard floor at -1.0 and the polar nullzone forcing at |z|>400k are NOT in scope for this contract — they belong to Phase 3 (polar nullzone biome).

## Test Plan

JUnit 5 unit tests in `ZClampedGradientTest.java`:

- `testFromValueAtMinZ`: z = from_z exactly returns from_value (e.g., from_z=0, to_z=300000, from_value=1.0, to_value=-1.0; z=0 → 1.0)
- `testToValueAtMaxZ`: z = to_z exactly returns to_value (z=300000 → -1.0)
- `testMidpoint`: z = (from_z + to_z) / 2 returns (from_value + to_value) / 2 (z=150000 → 0.0)
- `testQuarterPoint`: z = from_z + 0.25 * (to_z - from_z) returns from_value + 0.25 * (to_value - from_value)
- `testClampBelowMin`: z << from_z returns from_value (z=-1000000 → 1.0)
- `testClampAboveMax`: z >> to_z returns to_value (z=1000000 → -1.0)
- `testInvertedRange`: from_z > to_z works correctly (function still monotonic between bounds)
- `testNegativeZRange`: from_z=-300000, to_z=0 with appropriate value mapping
- `testCodecRoundTrip`: serialize a ZClampedGradient instance to JSON, deserialize, verify equality
- `testRegistrationKey`: registered type's key is `ResourceLocation.parse("mc_fork:z_clamped_gradient")`

Coverage target: ≥90% of `ZClampedGradient.java` (math is small and fully testable).

Integration verification (manual, captured in Review Log):

- Build the mod, copy datapack to a test world's `datapacks/` directory
- Create a new world, spawn at z=0
- Use `/locate biome minecraft:plains` and `/locate biome minecraft:taiga` and verify approximate distance to taiga differs noticeably from vanilla (taiga should generate at smaller |z| due to negative bias)
- Travel to z=200000, observe biome change toward cold variants
- This is a sanity check, not a precise test — captured as before/after observations in the Review Log

## CI Gates

From `mod/`:

- `./gradlew build` exits 0
- `./gradlew test` exits 0; all `ZClampedGradientTest` tests pass
- `./gradlew jacocoTestReport` exits 0; coverage of `ZClampedGradient.java` ≥ 90%
- `./gradlew runData` exits 0
- `./gradlew runClient --no-daemon` launches; mod loads; no errors related to density function registration in startup log

Datapack JSON validation: `latitude_bias.json` and `temperature.json` parse without errors when the test world loads (verified by absence of "Failed to load worldgen" lines in client log).

## Security Scope

No security-sensitive surfaces. Density function math, JSON parsing of trusted datapack files, no user input, no network, no auth. Security review not required.

## Parallelization Strategy

The teammate executing this contract MUST dispatch subagents for the independent subtask groups below. Sequential execution of these groups is a planning failure.

- **Group A (parallel via subagent):** Write `ZClampedGradientTest.java` (RED) — tests fail because class does not exist
- **Group B (parallel via subagent):** Write `ZClampedGradient.java` math + codec
- **Group C (parallel via subagent):** Write `ModDensityFunctions.java` registration class
- **Group D (parallel via subagent):** Update `McForkMod.java` to invoke registration
- **Group E (parallel via subagent):** Update `latitude_bias.json` to use new type
- **Group F (sequential, depends on A-E):** Run `./gradlew build && ./gradlew test`; iterate on failures
- **Group G (sequential, depends on F):** Manual integration verification with `runClient`; capture log + biome locate output for Review Log

## Checklist

- [ ] `ZClampedGradientTest.java` written first (RED) — tests fail at compile or runtime because `ZClampedGradient` does not exist yet
- [ ] `ZClampedGradient.java` written; tests pass (GREEN)
- [ ] Coverage of `ZClampedGradient.java` ≥ 90%
- [ ] `ModDensityFunctions.java` written with `DeferredRegister` for `BuiltInRegistries.DENSITY_FUNCTION_TYPE`
- [ ] `McForkMod.java` updated to invoke `ModDensityFunctions.register(modBus)` in constructor
- [ ] `datapack/data/mc_fork/worldgen/density_function/latitude_bias.json` updated to use `mc_fork:z_clamped_gradient`; placeholder comment removed
- [ ] `./gradlew build` exits 0
- [ ] `./gradlew test` exits 0
- [ ] `./gradlew jacocoTestReport` shows `ZClampedGradient.java` coverage ≥ 90%
- [ ] `./gradlew runClient` launches successfully; no density function registration errors in log
- [ ] Manual integration check: spawn world, observe biome distribution at z=0 vs z=200000; record observations in Review Log
- [ ] Commit and push to `main`

## Acceptance Criteria

1. `./gradlew build` exits 0 from `mod/` directory
2. `./gradlew test` exits 0; all 10 tests in `ZClampedGradientTest` pass (one per Test Plan bullet)
3. JaCoCo report shows `ZClampedGradient.java` line coverage ≥ 90%
4. The string `mc_fork:z_clamped_gradient` appears in the registered density function type registry at runtime (verified via reflection or `/data get` command in test world)
5. `latitude_bias.json` parses without error when loaded by Minecraft (no "Failed to parse density_function" lines in client log)
6. Override of `minecraft:overworld/temperature` at `datapack/data/minecraft/worldgen/density_function/overworld/temperature.json` continues to parse without error
7. Manual integration observation logged in Review Log: biome at world spawn at z=0 differs from biome at z=200000 in a direction consistent with cooling (taiga / snowy biomes more prevalent at higher |z|)
8. No hardcoded literals in `ZClampedGradient.java` other than mathematical constants (no magic numbers, no embedded strings beyond resource location)
9. File `ZClampedGradient.java` is ≤ 200 lines

## Review Log

| Date | Reviewer | Role | Verdict | Comments |
|------|----------|------|---------|----------|

## Revision History

| Date | Author | Change | Reason |
|------|--------|--------|--------|
| 2026-05-03 | orchestrator | Created | Initial draft for Phase 1 latitude DF activation |
